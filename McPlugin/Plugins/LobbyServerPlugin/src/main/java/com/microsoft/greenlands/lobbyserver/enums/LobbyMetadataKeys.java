package com.microsoft.greenlands.lobbyserver.enums;

/**
 * Enum that represents the possible metadata fields that can be stored in an entity in the Lobby.
 */
public enum LobbyMetadataKeys {
  TASK_CHOICE,
  SIGN_NAME
}
